Django==5.0.6
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.4.0
django-environ==0.11.2
django-filter==24.2
django-storages==1.14.3
django-celery-beat==2.6.0
django-celery-results==2.5.1

# Database
psycopg[binary]==3.2.9

# WSGI server + static file serving
gunicorn==22.0.0
whitenoise[brotli]==6.7.0

# Cache / Queue
redis==5.0.7
celery==5.4.0

# API Documentation
drf-spectacular==0.27.2

# Security & Auth
argon2-cffi==23.1.0
# Verifies Firebase ID tokens in accounts.views.SocialAuthView. Social login is
# the app's only sign-in path, so these are required, not optional.
google-auth==2.32.0
PyJWT==2.8.0

# Storage (S3-compatible for Cloudflare R2)
boto3==1.34.144

# Image processing
Pillow>=10.4.0

# Optional: automatic background removal on product image upload.
# rembg pulls onnxruntime (~200MB), which roughly triples the image size, so it
# is not installed by default. products.views imports it lazily and falls back
# to saving the original image, so leaving it out only disables that one
# feature. Install with:  pip install -r requirements-imaging.txt

# Utilities
python-slugify==8.0.4
phonenumbers==8.13.42
requests==2.32.3
# products.views / stores.views read OPENROUTER_* through decouple's config().
python-decouple==3.8

# Admin theme
django-jazzmin==3.0.5

# Development and test packages live in requirements-dev.txt so they stay out
# of the production image:  pip install -r requirements.txt -r requirements-dev.txt

