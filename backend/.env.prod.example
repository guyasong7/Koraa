# Koraa — Django production environment.
#
#   cp backend/.env.prod.example backend/.env.prod   # then fill in every value
#
# Loaded into the backend, migrate, celery_worker and celery_beat containers by
# docker-compose.prod.yml (env_file). Unlike .env.prod at the repo root, compose
# does not interpolate this file — it passes the lines through verbatim, so
# ${...} references do NOT work here.
#
# backend/.env.prod is gitignored, and backend/.dockerignore keeps every .env
# out of the image. Never commit the filled-in copy.

# ─── Core Django ──────────────────────────────────────────────────────────────
# Required. SimpleJWT signs access tokens with this, so a leaked or guessable
# key lets anyone mint a token for any merchant. production.py refuses to boot
# if it is shorter than 50 characters or still has the django-insecure- prefix.
#   python -c 'import secrets; print(secrets.token_urlsafe(64))'
SECRET_KEY=

DEBUG=False

# Required. Every hostname the backend answers on. production.py refuses to boot
# while this is still the development default.
#
# Two entries are not public domains and are both needed:
#   backend    — the compose service name, used by Next.js server components and
#                by nginx's upstream.
#   127.0.0.1  — the image's HEALTHCHECK curls http://127.0.0.1:8000/api/schema/
#                from inside the container. Without it get_host() raises
#                DisallowedHost, every check gets a 400, and the container is
#                reported unhealthy forever while serving traffic perfectly well.
#
# CSRF_TRUSTED_ORIGINS is derived from this list, minus localhost and 127.0.0.1.
ALLOWED_HOSTS=koraa.cm,www.koraa.cm,api.koraa.cm,.koraa.cm,backend,127.0.0.1

# ─── Database & Redis ─────────────────────────────────────────────────────────
# Deliberately absent: docker-compose.prod.yml sets DATABASE_URL and REDIS_URL
# as container environment, which wins over anything written here. Set them only
# when pointing at a managed database outside this stack — and remember that a
# value here is then shadowed by compose unless you also edit the compose file.

# ─── Domains ──────────────────────────────────────────────────────────────────
# The apex domain, BARE — no scheme, no trailing slash, no port. Django builds
# the CORS origins from it by interpolation (f"https://{KORAA_ROOT_DOMAIN}") and
# the subdomain regex by escaping its dots, so a value like
# "https://example.com/" yields the origin "https://https://example.com/" and a
# regex that matches nothing. Every storefront request is then CORS-blocked, and
# the browser reports it as a network failure rather than as a bad setting.
KORAA_ROOT_DOMAIN=koraa.cm

# Suffix used when building a new store's default storefront URL.
KORAA_STOREFRONT_DOMAIN=koraa.cm

# Absolute base URL used in outbound email links (verification, password reset,
# staff invitations). A wrong value sends users to a dead link, so it needs the
# scheme and no trailing slash.
KORAA_DASHBOARD_URL=https://koraa.cm

# ─── CORS & CSRF ──────────────────────────────────────────────────────────────
# Both are derived automatically: CORS from KORAA_ROOT_DOMAIN (apex, www, and a
# regex covering every subdomain), CSRF_TRUSTED_ORIGINS from ALLOWED_HOSTS.
# Setting either REPLACES the derived list rather than adding to it.
#
# Custom merchant domains (StoreDomain records) are not covered by the subdomain
# regex. Either list them here, or set KORAA_PUBLIC_API_URL=/api/v1 so storefront
# requests are same-origin and CORS never applies.
#
# Vercel preview deployments are not covered either — they are served from
# *.vercel.app, which is not a subdomain of the root domain. Add the specific
# preview origin here while testing one against this API.
# CORS_ALLOWED_ORIGINS=https://koraa.cm,https://shop.example.com
# CORS_ALLOWED_ORIGIN_REGEXES=^https://[a-z0-9\-]+\.koraa\.cm$
# CSRF_TRUSTED_ORIGINS=https://koraa.cm,https://api.koraa.cm

# ─── HTTPS ────────────────────────────────────────────────────────────────────
# nginx already redirects HTTP to HTTPS, and Django trusts X-Forwarded-Proto, so
# leaving this True is correct and costs nothing. Set it False only when
# something in front terminates TLS and cannot forward that header — otherwise
# Django sees plain HTTP and redirect-loops the client.
SECURE_SSL_REDIRECT=True

# ─── Email ────────────────────────────────────────────────────────────────────
# The default backend prints to the console, which in production means
# verification and password-reset mail is never delivered. Configure real SMTP.
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.resend.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=resend
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=Koraa <noreply@koraa.cm>

# ─── Firebase (server-side token verification) ────────────────────────────────
# The backend verifies Firebase ID tokens against this project id. It MUST name
# the same project as NEXT_PUBLIC_FIREBASE_PROJECT_ID in .env.prod, or the
# audience check fails and every social login is rejected. Note that base.py has
# a hardcoded default here — set it explicitly so a project rename cannot leave
# the stale default in place silently.
FIREBASE_PROJECT_ID=

# ─── Fapshi (payments & payouts) ──────────────────────────────────────────────
# From the Fapshi dashboard. Empty credentials do not fail at boot — checkout
# fails later at the first API call instead, so verify these before launch.
#
# BASE_URL selects the environment and has NO default: leaving it empty raises
# ImproperlyConfigured at the first payment rather than quietly charging real
# money, which is what the old live default did. https://live.fapshi.com moves
# real money; https://sandbox.fapshi.com does not, and has documented test
# numbers with deterministic outcomes. Sandbox and live keys are not
# interchangeable.
#
# Live also needs direct-pay switched on for the account — it ships disabled, and
# until Fapshi enables it every charge comes back 403. Sandbox has it on.
#
# If IP whitelisting is enabled on the account, this server's public IP must be
# on the list: without it, charges and payouts 403 while status checks keep
# working, which reads exactly like a bad key.
FAPSHI_API_USER=
FAPSHI_API_KEY=
FAPSHI_BASE_URL=

# Fapshi's minimum for one transaction, in XAF. Only change this if Fapshi does.
FAPSHI_MIN_AMOUNT=100

# The static string Fapshi sends back in the `x-wh-secret` header of every
# webhook, set alongside the webhook URL in the dashboard. It cannot be read back
# after you save it, so record it then. Optional: webhooks are verified by
# re-asking Fapshi for the payment's status, never by trusting the payload, so an
# empty value degrades nothing — it only skips the extra check.
FAPSHI_WEBHOOK_SECRET=

# ─── Didit (merchant identity verification) ───────────────────────────────────
# x-api-key for verification.didit.me. Merchant onboarding raises
# "DIDIT_API_KEY is not configured" without it.
DIDIT_API_KEY=

# ─── OpenRouter (AI product & store copy generation) ──────────────────────────
# Read through python-decouple, not django-environ. The AI description endpoints
# return a 4xx explaining the key is missing rather than breaking anything else,
# so this is safe to leave empty if the feature is not being shipped.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=openai/gpt-4o-mini

# ─── Media storage ────────────────────────────────────────────────────────────
# Three options, and exactly one may be active at a time. Uploads default to the
# media_files docker volume, which nginx serves at /media/. That works, but it
# lives on one host: the volume is what has to be backed up, it does not survive
# moving to a second web node, and `docker compose down -v` erases it.
#
# ── Option 1: AWS S3 ──
# Used by the EC2 deployment. USE_S3=True switches media to the bucket named
# below and points MEDIA_URL at it.
USE_S3=False
# AWS_STORAGE_BUCKET_NAME=koraa-media-prod
# AWS_S3_REGION_NAME=us-east-1
#
# Leave both credentials empty when running on EC2. base.py turns empty into
# None, and boto3 then reads temporary credentials from the instance's IAM role —
# nothing long-lived is written to this file or to the disk, and rotation is
# automatic. Set them only when running somewhere without an instance role.
#
# One EC2 detail if uploads fail with NoCredentialsError despite a correct role:
# IMDSv2 defaults to a PUT response hop limit of 1, and a container on the bridge
# network is one hop further out than the host, so the token request is dropped.
# The instance needs HttpPutResponseHopLimit=2.
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
#
# Defaults to <bucket>.s3.<region>.amazonaws.com. Override to serve media through
# CloudFront or a CNAME instead of the bucket's own hostname.
# AWS_S3_CUSTOM_DOMAIN=media.koraa.cm
#
# Objects are written under media/ with no ACL — buckets created since April 2023
# have ACLs disabled, and any ACL is rejected outright. Public read comes from a
# bucket policy scoped to media/*, so nothing else in the bucket is exposed.
#
# ── Option 2: Cloudflare R2 ──
# All four values are required together. Partial configuration is ignored and
# falls back to local disk rather than refusing to boot, so a typo here shows up
# as uploads quietly landing on the wrong side.
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_BUCKET_NAME=
# R2_ENDPOINT_URL=https://<account_id>.r2.cloudflarestorage.com
# Public hostname bound to the bucket. Without it, URLs use the R2 endpoint,
# which is not a good public URL.
# R2_CUSTOM_DOMAIN=media.koraa.cm
#
# S3 and R2 are the same storage backend with different endpoints and cannot both
# be active. production.py raises ImproperlyConfigured if USE_S3 is true while
# the R2 values are set, rather than picking one and leaving the other's uploads
# to go missing.
#
# ── Option 3: the local volume ──
# USE_S3=False and no R2_* values. Nothing else to set.

# Static files are collected at image build time and served by WhiteNoise from
# inside gunicorn. Nothing to configure, and no collectstatic step on deploy.

# ─── Gunicorn ─────────────────────────────────────────────────────────────────
# Starting point is (2 x cores) + 1. Each worker is a separate process holding
# its own DB connections, so raising this raises Postgres connection use too.
GUNICORN_WORKERS=3
GUNICORN_THREADS=2
# Must stay above the 15s timeout the Fapshi calls use, or a slow payment
# confirmation is killed mid-flight.
GUNICORN_TIMEOUT=60
GUNICORN_LOG_LEVEL=info

# ─── Logging ──────────────────────────────────────────────────────────────────
# Goes to stdout for the container runtime to collect: docker compose logs.
DJANGO_LOG_LEVEL=INFO

# Reuse each Postgres connection for this many seconds instead of opening one
# per request.
CONN_MAX_AGE=60
